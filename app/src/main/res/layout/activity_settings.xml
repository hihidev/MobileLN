<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".SettingsActivity">
    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:layout_marginEnd="15dp"
                  android:layout_marginStart="15dp"
                  android:orientation="vertical">
        <android.support.design.button.MaterialButton android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="About me"
                                                      android:textSize="30sp"/>
        <android.support.design.button.MaterialButton android:id="@+id/settings_bitcoind_log"
                                                      android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="(Debug) Bitcoind log"
                                                      android:textSize="30sp"/>
        <android.support.design.button.MaterialButton android:id="@+id/settings_lightningd_log"
                                                      android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="(Debug) Lightningd log"
                                                      android:textSize="30sp"/>
        <android.support.design.button.MaterialButton android:id="@+id/settings_bitcoind_config"
                                                      android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="(Dangerous) Bitcoind config"
                                                      android:textSize="30sp"/>
        <android.support.design.button.MaterialButton android:id="@+id/settings_lightningd_config"
                                                      android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="(Dangerous) Lightning config"
                                                      android:textSize="30sp"/>
        <android.support.design.button.MaterialButton android:id="@+id/settings_lightning_cli_console"
                                                      android:layout_width="match_parent"
                                                      android:layout_height="wrap_content"
                                                      android:text="(Dangerous) lightning-cli console"
                                                      android:textSize="30sp"/>
    </LinearLayout>
</RelativeLayout>